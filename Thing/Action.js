/** Action: Run an action.
 *
 * @param {thing.Action.target} thing.Action.target which is a turd.
 * @returns {Thing}
 */
export const Action = thing => thing.Action.target(thing)

export default Action
